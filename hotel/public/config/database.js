const config = {
    Hotel:{
        // 数据库配置
        database: {
            DATABASE: 'hotel',
            USERNAME: 'root',
            PASSWORD: 'abcd234',
            PORT: '3306',
            HOST: '127.0.0.1'
        }
    }
};

module.exports = config;